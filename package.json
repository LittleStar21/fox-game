{
  "name": "fox-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url ./",
    "format": "prettier --ignore-path ./.gitignore --write \"src/**/*.{html,css,js,json}\"",
    "format:check": "prettier --ignore-path ./.gitignore --check \"src/**/*.{html,css,js,json}\"",
    "lint": "eslint --ignore-path ./.gitignore --fix \"src/**/*.js\"",
    "lint:check": "eslint --ignore-path ./.gitignore --quiet \"src/**/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.12.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-import": "^2.25.4",
    "parcel-bundler": "^1.12.5",
    "prettier": "^2.6.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
